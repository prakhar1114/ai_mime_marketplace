# Fallback Plan

Intent: complete the Swiggy cart setup manually if the browser-harness script cannot finish.

- Open Swiggy in the same browser session and make sure the user is signed in.
- Search for the requested restaurant and choose the closest matching result.
- Search within the menu for the requested dish, choose the closest matching available item, and add the cheapest acceptable customization if a modal appears.
- Open checkout and select the saved address labelled A2.
- Stop once checkout is ready. Do not place the order or pay.
